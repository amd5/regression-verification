# 回归验证技能维护规则

- 本仓库任一文件发生更新时，必须在同一任务完成验证、提交、推送版本标签和 GitHub Release 回读。
- 标准入口是 `python scripts/release_skill.py --message "<本次更新说明>"`，不兼容迁移增加 `--bump major`，不得把只编辑本地文件视为完成。
- 发布前必须检查最终差异，只允许提交本技能仓库内与当前更新相关的内容，禁止提交凭据、密钥、环境文件、目标业务项目或治理中间文件。
- 只有用户最新指令明确禁止外部写入时才停止在本地修改，并明确报告未提交、未推送、未发布状态。
